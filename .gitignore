# --- Python ---
__pycache__/
*.py[cod]
*.pyd
*.so
*.egg-info/
.venv/
venv/

# --- Env / secrets ---
# .env is tracked intentionally for PC1/PC2 sync (paper trading only — no real keys)
# Remove this comment and uncomment the lines below before adding real API credentials
# .env
*.env
.env.*

# --- Runtime outputs ---
logs/
ops/logs/
*.log
*.csv
*.json

# --- Candle data (keep in repo for PC2 sync) ---
!data/eth_usd_1m.csv
!data/btc_usd_1m.csv
!data/sol_usd_1m.csv

# --- Backtest artifacts ---
bt_summary_latest.json
bt_summary_*.json
backtest/trades_debug.csv
backtest/trades_from_events.csv

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
