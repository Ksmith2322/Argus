

!argus_flow/configs/*.json
!data/btc_usd_1m.csv
!data/eth_usd_1m.csv
!data/sol_usd_1m.csv
!ops/coin_pool.json
!helio/backtest_factory/configs/*.json
# --- Argus Flow (Cascade/IBKR) ---
# --- Backtest artifacts ---
# --- Candle data (keep in repo for PC2 sync) ---
# --- Env / secrets ---
# --- OS / editor ---
# --- Runtime flag files (operational state, not source) ---
# --- Runtime outputs ---
# .env
# .env is tracked intentionally for PC1/PC2 sync (paper trading only — no real keys)
# Coinbase API credentials
# Data files too large for git (re-downloadable)
# Forge runtime data (databases, generated artifacts)
# IB Gateway installer
# Keep configs in git
# Remove this comment and uncomment the lines below before adding real API credentials
*.csv
*.egg-info/
*.env
*.json
*.lock
*.log
*.pidlock
*.py[cod]
*.pyd
*.so
.DS_Store
.env.*
.env.coinbase
.pycache_verify/
.tmp*/
.tmp_*/
.venv/
.vscode/
KILL_SWITCH
PAUSE_ENTRIES
Thumbs.db
__pycache__/
_tmp/
argus_flow/data/
argus_flow/logs/
argus_flow/replay_out/
backtest/trades_debug.csv
backtest/trades_from_events.csv
bt_summary_*.json
bt_summary_latest.json
cdp_api_key.json
forge/data/
forge/logs/
ibgateway-*.exe
logs/
ops/logs/
pytest-cache-files-*/
secrets/
venv/
﻿# --- Python ---
