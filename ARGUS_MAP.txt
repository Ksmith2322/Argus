Here is the **fully updated ARGUS_MAP** aligned to your current `repo` package layout (no more `nova_scripts` namespace, legacy entrypoints marked explicitly hazardous).

---

# ARGUS MAP (call graph + sources of truth)

===============================================================================

1. ENTRYPOINTS (commands actually run)
   ===============================================================================

LIVE (PRIMARY):

* python -m repo.runner_live

BACKTEST (PRIMARY):

* python -m repo.backtest.runner

LEGACY / DO NOT USE:

* python -m repo.main                      (calls old nova_scripts path)
* python -m repo.run_main                  (hardcoded nova_scripts shim)
* python repo/backtest/run_backtest.py     (legacy sys.path shim)

DATA INGEST (MANUAL TOOLING):

* python -m repo.backtest.download_candles
  (Coinbase REST candles → CSV in repo/backtest/data/)

DEBUG:

* python -m repo._debug_step

===============================================================================
2) CANONICAL SOURCES OF TRUTH (ONLY ONE EACH)
=============================================

Snapshot schema / event objects:

* DecisionSnapshot
  → decisions.py

Signal + event CSV schema AND writers:

* CANONICAL:
  → io_logs.py
* NON-CANONICAL (DUPLICATE / DRIFT RISK):
  → logger.py  (legacy; should not be used)

Bot state ownership (all sub-engines live here):

* BotState
  → state.py

Position, fills, exposure, PnL accounting:

* ledger.py
* pnl_shadow.py (math helpers only)

Risk gates / daily limits / cooldowns:

* risk.py

Signal generation (per timeframe):

* strategy_phase2.py

Confluence scoring:

* confluence.py
* adaptive_confluence.py (regime overlay)

Liquidity filters + metrics (Phase 5B):

* liquidity.py
  (spread, vol_1m, vol_baseline, atr_norm, penalties)

Market structure (Phase 5A):

* structure.py

Session classification + modifiers (Phase 5C):

* session.py

Regime detection:

* regime.py

Candle aggregation:

* candles.py

Indicators:

* indicators.py

===============================================================================
3) CORE CALL CHAINS (WHAT CALLS WHAT)
=====================================

LIVE LOOP:

repo.runner_live
-> feed_coinbase.py           (HTTP spot + preload)
-> state.BotState             (owns sub-engines)
-> engine.step(...)
-> candles.py             (1m / 5m / 1h builders)
-> indicators.py
-> strategy_phase2.py
-> structure.py
-> liquidity.py
-> session.py
-> regime.py
-> confluence.py
-> adaptive_confluence.py
-> ledger.py
-> risk.py
-> trade_tracker.py       (optional)
-> decisions.py           (DecisionSnapshot + EngineEvent)
-> io_logs.py                 (write live_signals.csv, live_events.csv)
-> notify.py                  (Discord webhook)

BACKTEST LOOP:

repo.backtest.runner
-> backtest.loader            (CSV → CandleRow)
-> backtest.feed              (CandleRow → PriceTick)
-> state.BotState
-> engine.step(...)
-> io_logs.py                 (bt_signals.csv, bt_events.csv)
-> backtest.results           (derive metrics from events)

TOOLS:

backtest.download_candles
-> requests → CSV in repo/backtest/data/

backtest.from_live_events
-> parse live_events.csv → reconstruct trades

_debug_step
-> single engine.step() with one tick

===============================================================================
4) I/O SURFACES (WHERE FILES ARE WRITTEN)
=========================================

repo/logs/  (CANONICAL WRITERS ONLY — io_logs.py)

* live_signals.csv
* live_events.csv
* bt_signals.csv
* bt_events.csv

repo/backtest/data/

* candle CSVs from download_candles

optional:

* repo/trade_tracker.txt
  → trade_tracker.py

optional legacy:

* repo/logs/argus_logger_debug.log
  → logger.py (should be removed eventually)

KILL / PAUSE FILES (relative to CWD):

* KILL_SWITCH.txt
* PAUSE.txt

===============================================================================
5) CONFIG & ENV KNOBS (MATERIAL BEHAVIOR CHANGES ONLY)
======================================================

Execution / timing:

* BACKTEST_MODE
* CANDLE_SECONDS
* STALE_TICK_SECONDS

Liquidity (Phase 5B):

* USE_LIQUIDITY_FILTERS / USE_LIQUIDITY
* LIQ_MODE (BLOCK | PENALIZE)
* LIQ_MAX_SPREAD_BPS
* LIQ_MIN_VOL_1M
* LIQ_MIN_VOL_MULT
* LIQ_VOL_BASELINE_WINDOW
* LIQ_USE_SYNTHETIC_SPREAD
* LIQ_SYNTH_SPREAD_FLOOR_BPS
* LIQ_SYNTH_SPREAD_ATR_MULT_BPS

Backtest-only helpers:

* BT_SYNTH_SPREAD_BPS
* BT_TRUNCATE_LOGS
* BT_WRITE_LOGS

Confluence:

* CONFLUENCE_MIN_SCORE
* REQUIRE_CONFLUENCE
* USE_SHOULD_EVENTS

Risk / sizing:

* USD_PER_TRADE
* START_CASH_USD
* MIN_HOLD_SECONDS
* daily limits / cooldown parameters (risk.py)

Observability:

* DISABLE_TRADE_TRACKER_IN_BACKTEST
* VERIFY_TF_SNAPSHOTS

Adversarial testing:

* ARGUS_PROFILE
  (regime forcing / liquidity forcing / scoring perturbation)

===============================================================================
6) KNOWN HAZARDS (CURRENT REALITY)
==================================

* logger.py duplicates CSV writing → schema drift risk
* main.py and run_main.py reference old nova_scripts namespace
* Relative ./logs path depends on launch directory
* Summary metrics must be derived from events (not parallel counters)
* vol_baseline requires update_on_1m_close() firing correctly
* atr_norm may be None in backtest unless computed or synthesized
* trade_tracker writes side-effect file (can desync from events)

===============================================================================
7) CURRENT STATUS (END OF THIS SESSION)
=======================================

* Running via: python -m repo.runner_live
* Backtest via: python -m repo.backtest.runner
* Package namespace normalized to repo
* Legacy nova_scripts shims still present but isolated
* Liquidity baseline wiring verified
* Synthetic spread stable in backtest
* ENTRY_ATTEMPT / ENTRY_METRICS instrumentation verified
* Open architectural cleanup:
  remove logger.py OR enforce single-writer policy
  delete legacy entrypoints (main.py, run_main.py, backtest/run_backtest.py)
  harden working-directory invariants (avoid ./logs drift)

---

If you want next step:
We can now collapse this into a **Phase 6 Architecture Freeze** version that eliminates legacy shims entirely and reduces the system to one canonical execution surface.
