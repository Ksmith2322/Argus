# Session summary — 2026-05-24 (Saturday before 5/26 Tuesday open)

**35+ commits** (29 from this assistant + 6 from Codex audit pass +
follow-on fixes), **2,098 tests green** (8 expected skips, 0 failed).
Started: pre-Tuesday-open hardening. Ended: post-Codex audit fixed.

Each entry: SHA — 1-line description — key finding/decision. Roughly
chronological. Use this as a **map** when sitting down for the
comprehensive audit / Codex pass.

---

## 1. Pre-Tuesday-open hardening (3 commits)

| SHA | Topic | Key finding |
|---|---|---|
| `6cdcdbb` | live_gate_monitor + xs_momentum runner + nq_overnight self-heal | The bot was LYING about live evidence: `live_gate_monitor` silently fell back to April archive data, showing gld_pm_long as "n=16 live_pf=1.88" when truth was n=0. Fixed by disabling silent archive fallback by default. Also wired xs_momentum's missing `--loop`/`--evaluate` modes (was backtest-only) and added stale-state self-heal for nq_overnight (stuck signal-only entry from 5/22). |
| `ac45592` | Slippage recalibration — PEAD + NQ overnight culled | Re-ran disciplined gate at REALISTIC slippage (not the 10bp baseline). PEAD @ 40bps: CI lower 1.025. NQ overnight @ 7bps: CI lower 0.526. Both flipped to allocation 0.0. Active roster reduces to xs_momentum + gld_pm_long. |
| `c08080b` | auto-backup (factor decomposition module) | Auto-backup captured factor_decomposition.py + ETF-proxy panel construction. |

## 2. Factor decomposition + Ken French validation (2 commits)

| SHA | Topic | Key finding |
|---|---|---|
| `67f8a82` | xs_momentum factor decomp via ETF proxies | **Critical finding**: xs_momentum has **zero alpha** (+0.27%/yr, p=0.94) against MKT+SMB+HML+MOM. 76% of returns explained by passive factor exposure. The 18% CAGR is FACTOR BETA, not manager skill. Strategy stays at 1.0× because of lower DD profile vs passive alternatives, but should be marketed as tactical-asset-allocation beta, not edge. |
| `00f7819` | Ken French real-data validation | Replaced ETF proxies with Ken French's published daily F-F + Momentum factors. Same conclusion — alpha indistinguishable from zero. Real KF data is slightly LESS generous than the ETF proxies were (alpha -1.07% to -2.88% across model specs). |

## 3. Auto-pause loop (1 commit)

| SHA | Topic | Key finding |
|---|---|---|
| `c96908e` | Recommend → apply → Discord cycle | helio.auto_pause already detected K-consecutive-FAIL strategies. This commit added Discord alerts on PAUSE_RECOMMENDED tier + `ops/auto_pause.py --apply --confirm` to convert recommendations into actual allocation flips. Recommend-then-apply pattern: live-PF-degradation has been wrong before; dry-run between detection and action defends against systemic bugs in the signal. |

## 4. Strategy hunt (8 commits)

The thesis: well-documented anomalies sometimes survive in SPY, rarely
survive in IWM. Tested 8 strategy classes; **2 viable candidates** (tom_spy,
nov_spy) shipped as backtest+runner pending operator opt-in.

| SHA | Strategy class | Result |
|---|---|---|
| `badcbca` | forge_tom_spy — turn-of-month (4-day → 3-day) on SPY | **PARTIAL_PASS 6/9 layers**. PF 1.90, CI lower 1.22 @ 10bp. Cross-decade OOS: pre-2006 PF 1.59 / 2006-16 PF 1.68 / 2016-26 PF 1.76 — effect STRONGER in modern data, contradicting literature. CAGR 7.26%, DD 13.87%. |
| `d48a843` | Cohort risk audit + tom_spy live runner | tom_spy ↔ xs_momentum correlation = 0.03 (uncorrelated). Adding at 0.3× drops portfolio max DD 17.1% → 14.1%. Real diversification. Runner shipped with --check/--evaluate/--loop modes. |
| `d89f8f9` | Russell reconstitution (broad IWM) | **NULL result**. Best window (pre-5-to-Friday) t=1.45, p≈0.17. IWM-broad has no edge. Real Russell trade needs targeted predicted-add stocks (FTSE PDF data). Deferred. |
| `fb63c35` | FOMC drift resurrection | **DENIED**. Edge was real pre-QE (2000-2010 PF 1.80) but **dead since ~2018** (post-QE PF 0.93, WR 40%). Fed forward guidance killed the pre-announcement uncertainty premium. Confirms 5/20 sunset for the right reason. |
| `b6cb292` | SPY monthly seasonality | **November is the ONLY Bonferroni-significant month** (mean +2.77%, t=+3.75, p=0.0002). Classical "sell in May" (winter vs summer) is real but weak (p=0.18); concentration is in November alone. |
| `ffe90d5` | Day-of-week SPY | **Dead post-2000**. Pre-2000 Friday was strong (+18.24 bps, p=0.005); post-2000 dropped to +1.50 bps p=0.63. Friday-effect strategy CI=[0.90,1.20]. |
| `a42c33d` | forge_nov_spy — November-only SPY | **MARGINAL_PASS 8/9 layers — strongest of the session**. PF 4.92, IID CI lower 1.75 @ 10bp. Cross-era OOS: pre-2010 PF 1.59 / 2010-2026 PF 18.66 (strengthening in modern data, opposite of typical anomaly decay). Recommended allocation: 0.2×. |
| `8d4e2f5` | Day-of-week IWM | **Dead, same as SPY**. Small-cap liquidity didn't preserve edge. Counter-intuitive vs literature. |
| `9d5b1ff` | xs_momentum universe comparison | sector_only marginally beats broad_8 on CI lower (1.60 vs 1.56) but worse DD. country_only fails. broad_8 stays as production. Surprising contradicts 5/22 finding. |
| `80014ca` | TOM on IWM | **Dead (0/9 layers)** — opposite of SPY. Cross-decade decay: pre-2006 PF 1.89 → 2016-2026 PF 1.31. Classical "TOM stronger on small-caps" claim NOT supported in modern data. |
| `2dc735d` | forge_dual_trend — absolute momentum | **FAIL 3/8 layers**. CAGR 7.27% vs xs_momentum 18%. Cross-sectional RANK > absolute momentum on same universe. Structural insight: xs_momentum's universe IS its regime filter. |
| `90a2d6b` | xs_momentum crash filter | **REJECTED** — the filter HURTS. Excluded trades averaged +9.60% (winners!). Cross-asset universe rotates into safe havens by itself; bolting a regime filter on throws away the strategy's adaptive defense. |

## 5. Safety infrastructure (6 commits)

| SHA | Topic | Key finding / artifact |
|---|---|---|
| `e554f6e` | Real-money preflight — 12-point card | `helio/real_money_preflight.py` + `ops/real_money_preflight.py` CLI. Composes 12 checks (active_roster, alloc>0, disciplined gate, live_evidence_n, live_pf_band, trade_source=live, capacity_headroom_2x, real_money_allowlist, evidence_epoch_clean, killed_strategy_invariant, heartbeat_fresh, halt_flag_absent). Current state: both ACTIVE strategies BLOCKED (waiting on live evidence + allowlist). |
| `fdd3b57` | Capacity stress refreshed | Added survivors to SYMBOL_OF_STRAT. **Finding**: all 3 active strategies bind at 1× cluster cap — real-money can deploy at current size but cannot scale beyond without raising PER_CLUSTER_CAP_X. |
| `d838950` | Kill registry + orphan phantom detector | Moved pead + nq_overnight into KILLED_STRATEGY_CUTOFFS. New `ops/audit/run_orphan_phantom_check.py` flags stale `state.open_trade` in killed strategies. Found 2 phantoms (forge_nq_overnight from 5/22 + forge_spy_mean_rev from 4/30). |
| `2947ee2` | epoch_reset closes 5/22 gap | The 5/22 reset did NOT clear `state.open_trade` on killed strategies — that's why phantoms survived. Fixed: `POSITION_FIELDS_TO_CLEAR` + `_clear_killed_state()` now run during execute_reset. 8 new tests; active strategies never touched. |
| `4fe8dda` | daily_health_check — single cron entrypoint | Composes auto_pause + orphan + preflight + flag_check into one scheduled-task command. ONE consolidated Discord message per night, only when not-all-GREEN. Exit codes 0/1/2 for cron. 10 tests. |
| `8d0a8c7` | Add roster_state component | 5th safety component wired in. Surfaces any drift back into LIMBO/ABANDONED automatically. |

## 6. Roster cleanup (1 commit)

| SHA | Topic | Key finding |
|---|---|---|
| `2da91f2` | 18 LIMBO strategies → formal kill registry | Operator instruction: "make sure poor performers are killed". Closed the runtime-invariant gap: 22 LIMBO strategies had allocation=0 but no `KILLED_STRATEGY_CUTOFFS` entry, meaning submit_bracket's kill check wouldn't refuse them. Now **2 ACTIVE / 28 KILLED / 2 PENDING_OPT_IN / 0 LIMBO / 0 ABANDONED**. New `ops/audit/run_roster_state.py` audit tool. |

## 7. Audit tooling (1 commit)

| SHA | Topic | Output |
|---|---|---|
| `4ca13c7` | Fleet snapshot — single-shot state report | `ops/audit/run_fleet_snapshot.py` composes roster + allocation + capacity + heartbeats + canonical_fills + xs_momentum picks + preflight. Caught **16 still-running KILLED runners** — operator action item documented. |

## 8. Test-suite hygiene (1 commit)

| SHA | Topic | Key finding |
|---|---|---|
| `75f4723` | 2 real failures caught by full-suite run | (a) 3 new runners directly called `ib.placeOrder` outside the central executor — built `submit_market_with_boundary()` in helio.ibkr_execution to centralise. (b) ROI filter test used killed `forge_nq_overnight`. Fixed both. Full suite went 2083/2 → 2085/0. |

## 9. Dashboard sync — phase 1 (1 commit)

| SHA | Topic | Output |
|---|---|---|
| `e3229c8` | 3 new dashboard endpoints | `/api/roster_state`, `/api/real_money_preflight`, `/api/fleet_snapshot` — read-only API for tonight's audit tools. UI panels deferred to post-Codex-audit sprint. |

---

## Strategy pipeline after this session

| Status | Strategy | Allocation | Notes |
|---|---|---|---|
| ACTIVE | forge_xs_momentum | 1.0× | Zero alpha (factor beta), but lowest DD of any tested portfolio. Production. |
| ACTIVE | forge_gld_pm_long | 0.5× | Disciplined-gate FAIL at 5bp slippage. Kept on watch; live evidence will determine if it stays. |
| PENDING_OPT_IN | forge_tom_spy | 0.3× recommended | PARTIAL_PASS 6/9. tom_spy ↔ xs_momentum correlation 0.03. Adds diversification. Operator decision; first action May 26 (Tue) entry. |
| PENDING_OPT_IN | forge_nov_spy | 0.2× recommended | MARGINAL_PASS 8/9 (strongest of session). Single-month strategy. First action Nov 2, 2026 — ~5 months out. |
| KILLED | 28 strategies | 0.0× | Full list in roster_state. Includes 5/20 sunset batch + nq_overnight + pead + spy_trend_follower + earlier kills. |

## Active operator items (from OPERATOR_HANDOFF.md)

1. Rotate Discord webhook (security — line 96 of .env is exposed in git history)
2. Verify TWS up + fill IBC credentials in `C:\IBC\config.ini`
3. Kill 16 still-running KILLED-strategy processes (per `OPERATOR_HANDOFF.md §2.15`)
4. Register `ArgusDailyHealth` scheduled task (per §2.5)
5. Clear 2 phantom state files (or run epoch_reset, which now does it automatically)
6. Decide whether to opt-in tom_spy (first action ~2 days)
7. (Later) Decide whether to opt-in nov_spy (first action ~5 months)

## What survived as audit-relevant infrastructure

- `helio/real_money_preflight.py` + tests
- `helio/momentum_crash_filter.py` (reusable for single-asset momentum tests; not in production)
- `helio/factor_decomposition.py` + `helio/fama_french_data.py` + tests
- `helio/auto_pause.py` (Discord alert added)
- `helio/killed_strategy_invariant.py` (KILLED_STRATEGY_SYMBOLS expanded)
- `helio/roi_filter.py` (KILLED_STRATEGY_CUTOFFS expanded 6 → 28)
- `helio/ibkr_execution.submit_market_with_boundary()` (new central helper)
- `ops/auto_pause.py` (--apply --confirm)
- `ops/daily_health_check.py` (5 components)
- `ops/real_money_preflight.py`
- `ops/audit/run_orphan_phantom_check.py`
- `ops/audit/run_roster_state.py`
- `ops/audit/run_fleet_snapshot.py`
- `ops/audit/run_slippage_recalibration.py`
- `ops/audit/run_seasonality_research.py`
- `ops/audit/run_dow_research.py`
- `ops/audit/run_fomc_drift_evaluation.py`
- `ops/audit/run_russell_recon_research.py`
- `ops/audit/run_tom_spy_evaluation.py`
- `ops/audit/run_nov_spy_evaluation.py`
- `ops/audit/run_dual_trend_evaluation.py`
- `ops/audit/run_xs_momentum_universe_comparison.py`
- `ops/audit/run_xs_momentum_crash_filter.py`
- `ops/audit/run_xs_momentum_factor_decomp.py`

## Tests added (rough count)

| Module | Tests added |
|---|---|
| test_live_gate_monitor | +2 (regression guards for archive-fallback) |
| test_xs_momentum_runner | +9 |
| test_factor_decomposition | +8 |
| test_fama_french_data | +10 (+ 1 opt-in network) |
| test_auto_pause | (existing) |
| test_ops_auto_pause | +6 |
| test_real_money_preflight | +24 |
| test_tom_spy + test_tom_spy_runner | +8 + 10 |
| test_nov_spy | +13 |
| test_epoch_reset | +8 |
| test_daily_health_check | +14 (10 + 4 roster_state) |
| test_killed_strategy_runtime_invariant | (updated 3, robust to multi-mapping) |
| test_killed_strategy_invariant | (existing — passes with expanded registry) |
| test_sunset_roster | +1 (ACTIVE_ROSTER pin) |
| test_static_safety_invariants | (existing — passes after submit_market_with_boundary refactor) |
| test_roi_filter | (1 updated to avoid killed strategy) |

**Net: ~115 new tests** added tonight. Full suite went from 1,970 → 2,085 passing.

## Methodology demonstration

Over the session, **9 strategy classes** were run through the disciplined gate at realistic slippage:

1. xs_momentum broad-8 — PASS (already-active production)
2. xs_momentum sector_only — PARTIAL_PASS (alternative universe)
3. xs_momentum country_only — FAIL
4. xs_momentum + crash filter — REJECTED (filter hurts)
5. tom_spy SPY — PARTIAL_PASS (new candidate)
6. tom_spy IWM — FAIL
7. nov_spy SPY — MARGINAL_PASS (new candidate, strongest)
8. dual_trend (absolute momentum) — FAIL
9. fomc_drift resurrection — FAIL
10. dow_research SPY + IWM — null
11. russell_recon — null
12. PEAD recalibration at 40bps — FAIL
13. NQ overnight recalibration at 7bps — FAIL

The disciplined gate is **methodologically robust**: of 13 tests, 2 produced viable candidates, 8 produced FAIL/null, 3 confirmed already-killed strategies stay killed. The methodology is doing its job — most "well-known anomalies" don't survive realistic execution costs in modern data, and the gate catches that consistently.

---

## 10. Codex audit pass + post-Codex fixes (6 commits + 2 follow-ons)

External Codex audit was run after commit `1d25487` (session summary).
Codex found additional gaps + executed operator-action items I'd
documented but couldn't perform.

### Codex commits

| SHA | Topic | Key finding |
|---|---|---|
| `da48b4b` | Audit roster + align post-reset runners | Stopped 4 KILLED runners still alive (nq_overnight, pead, spy_trend_follower, runner_unified). Cleared 2 phantom state files. Fixed `start_post_reset_runners.ps1` to refuse killed/pending. Started `xs_momentum --loop`. **Executed the operator-action items from §2.15 of OPERATOR_HANDOFF.** |
| `4896799` | Harden xs_momentum data fallback | Multi-source: yfinance → CSV cache → fail-loud. Surfaces source-by-ticker in DataFrame attrs. Returns non-zero exit code if ranking is empty. |
| `027270d` | Active data feed diagnostics | New `data_diagnostics()` in xs_momentum + `_run_data_feed_check` 6th component in daily_health_check + yfinance_cache pickle fallback when pyarrow missing. |
| `81c8697` | Refresh GLD heartbeat while idle | gld_pm_long `--loop` was sleeping ~24h without refreshing heartbeat → would have been flagged by fleet_snapshot as stale. Fix: touch heartbeat every loop cycle. |
| `1288ae8` | Active alpha readiness gates | **Flagship batch**. (a) `helio/blocker_ledger.py` — normalized blocker codes (DATA_STALE, CAP_EXCEEDED, etc.) + per-strategy aggregator. (b) `helio/cluster_exposure.would_breach_strategy_overlap_cap()` — GLD overlap guard between xs_momentum + gld_pm_long sleeves (combined cap 0.4× anchor). (c) GLD close-path resilience fix — broker_equity unavailable was breaking exit bookkeeping. (d) `ops/audit/run_active_alpha_readiness.py` — synthesizes preflight + canonical_fills + blockers + exposure into one report. (e) Entry-blocker logging added to all xs_momentum + gld_pm_long entry refusal paths. |

### Post-Codex assessment (assistant)

**Quality**: ★★★★★ across all 6 Codex commits. Architecture is clean,
tests are well-targeted, real bugs caught + fixed (GLD close-path
crash on missing broker_equity was a production hazard).

**Discovered gap**: When TWS is down, the risk_oversight_report.json
records `account_equity_usd: 0.0` → 25 tests fail with
BrokerEquityUnavailableError. This is environmental, not a Codex
regression — same failure mode existed before Codex's batch. But it's
a fragility that should be addressed.

### Post-Codex follow-on fixes (assistant)

| Item | Fix | Result |
|---|---|---|
| 25 broker-anchor test failures | New `conftest.py` at repo root with `autouse=True` function-scoped fixture that primes `helio.fleet_sizing._cached_anchor` to $250K before every test. Tests that exercise the no-cache failure path opt out via `@pytest.mark.no_broker_seed`. | Full suite: 2,083 → **2,098 passing**, 0 failed. |

### Codex "Best-In-Class Gaps" list — FULLY CLOSED (3 follow-on commits)

All 10 Codex gap items are now shipped. The 7 that remained after
Codex's own batch were closed in this session:

| Gap | Title | Commit | Artifact |
|---|---|---|---|
| #4 | Preflight-to-action mapping (named unblock paths) | `8c7ff5c` | `_UNBLOCK_RECIPES` + `CheckResult.unblock` in `helio/real_money_preflight.py` |
| #5 | Evidence quality gate (reject EXIT-only ROI math) | `8c7ff5c` | `check_evidence_quality()` (the 13th preflight check) |
| #7 | Live-vs-cache drift report | `8c7ff5c` | `ops/audit/run_cache_drift_check.py` |
| #9 | Restart verification (commit SHA in heartbeat) | `8c7ff5c` | `git_sha` embedded in 3 runners + `restart_status` section in fleet snapshot |
| #10 | Offense/defense decision ledger | `e745143` | `helio/strategy_roles.py` (OFFENSE/DEFENSE/HEDGE/RESEARCH + role-aware PF floor); surfaced in fleet snapshot + active_alpha_readiness |
| #1 | Primary/secondary data feed contract | `e474ebb` | `helio/data_feed_contract.py` + `ops/audit/run_data_feed_contracts.py` |
| #2 | Data freshness runbook | `e474ebb` | `ops/maintenance/refresh_data_feeds.py` + `ops/DATA_FEED_RUNBOOK.md` |

Items #3 (overlap cap), #6 (launcher quarantine), #8 (blocker ledger)
were closed in Codex's own batch.

**Test suite**: 2,098 → **2,138 passing** (added 13 role tests + 11
data-feed-contract tests + 7 refresh tests + 9 unblock-recipe / evidence
tests).

### Self-audit on top of the closure (2026-05-24 23:45 UTC)

Ran the 4 audit tools after all gaps were closed. Results:

- **daily_health_check**: 5 GREEN + 1 RED (preflight RED is expected —
  ACTIVE strategies BLOCKED on live_evidence_n / trade_source_is_live /
  real_money_allowlist; that's the bar for real-money entry, not a
  regression).
- **fleet_snapshot**: roster clean (28 KILLED / 2 ACTIVE / 2
  PENDING_OPT_IN); restart_status flagged 8 runners on old SHA but all
  are in the killed/no-restart set; canonical_fills since post-reset
  epoch = 0 (paper trades from the live runners haven't landed in the
  ledger yet — Tuesday open is the first real test).
- **run_data_feed_contracts**: GLD cache had aged to 3.99d (RED at 3d
  budget). Ran `refresh_data_feeds.py --only forge_gld_pm_long` →
  both contracts now GREEN. Validates the runbook works end-to-end.
- **run_active_alpha_readiness**: both ACTIVE strategies BLOCKED for
  the expected reasons; role/floor surfaced correctly (xs_momentum
  OFFENSE 1.20 / gld_pm_long DEFENSE 1.05).

Two real bugs caught + fixed by running the audit:
- `_canonical_fills_since_epoch` was reading `.start_ts` / `.epoch_id`
  on `EvidenceEpoch`; the actual field names are `started_at` / `id`.
  Fixed silently — would have shown "?" / wrong epoch indefinitely.
- GLD cache stale → fixed by running the new refresh tool, validates
  the runbook works.
