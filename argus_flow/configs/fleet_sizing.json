{
  "_comment": "Central fleet sizing. Risk = risk_pct × broker_equity. Anchor is strictly dynamic from broker truth — no fallback, no ceiling. If broker is unavailable, runners enter READ_ONLY mode and refuse new entries (see BrokerEquityUnavailableError in helio/fleet_sizing.py). When switching paper→live, the same code path reads real broker equity — no config change needed.",
  "_architecture": "2026-04-23: removed fallback_anchor_usd and max_anchor_usd. Preprod now fully mirrors prod. IBKR paper account must be reset to the intended starting balance in Client Portal; there is no safety net.",

  "sizing_mode": "broker_equity_dynamic",

  "max_risk_pct_per_trade_ceiling": 0.03,
  "fleet_max_open_risk_pct": 0.06,

  "_tiers_note": "Confidence-based risk_pct. Each strategy's risk scales with measured live performance (last 90 days). Tiers evaluated per trade. Drawdown brake halves the tier if current dd > 50% of peak_pnl.",
  "tiers": [
    {"name": "unproven",    "min_valid_trades": 0,   "min_profit_factor": 0.0,  "risk_pct": 0.005},
    {"name": "emerging",    "min_valid_trades": 10,  "min_profit_factor": 1.00, "risk_pct": 0.010},
    {"name": "validated",   "min_valid_trades": 30,  "min_profit_factor": 1.20, "risk_pct": 0.015},
    {"name": "promoted",    "min_valid_trades": 60,  "min_profit_factor": 1.30, "risk_pct": 0.020},
    {"name": "exceptional", "min_valid_trades": 100, "min_profit_factor": 1.50, "risk_pct": 0.030}
  ],
  "tier_window_days": 90,
  "drawdown_brake_threshold": 0.5,

  "_strategy_live_cutoffs_note": "ISO-8601 timestamp per strategy. Trades with ts before this are excluded from tier stats. If unset, all trades count.",
  "strategy_live_cutoffs": {},

  "notional_caps_by_asset_class": {
    "stock": 0.3,
    "etf":   0.3,
    "fx":    1.0,
    "micro_future": 2.0
  },
  "_notional_caps_note": "Position notional_usd must be <= anchor × multiplier. Hard ceiling expressed as % of equity for stock/etf/fx. Futures get higher multiplier because they're risk-managed by margin (~10% of notional), not raw notional — 1 MNQ contract is ~$54K notional but only ~$5.4K margin = 18% of $30K equity. v8 (2026-04-27): bumped micro_future 0.5→2.0 to allow 1 contract on $30K anchor; otherwise nq_overnight, nq_london_close, mamba/tori/cuebanks couldn't trade at all. Stock/etf 0.3× and fx 1.0× kept tight for survival-first.",

  "version": "2026-04-27.v8"
}
