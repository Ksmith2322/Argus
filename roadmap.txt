# ARGUS ROADMAP
# Last updated: 2026-03-23
# Goal: Build multiple independent, automatable edge engines.
# Each must have measurable EV, be automatable, and scale capital.
# Strategy: narrow -> prove -> scale -> expand

---

# TRUTH-SURFACE DOCTRINE (architectural rule -- never violate)

Argus has three truth classes:

  1. Canonical execution truth
     orders, fills, account, positions, decision events, decision_trace

  2. Deterministic lifecycle truth
     trade lifecycle records rebuilt from canonical truth (trade_journal)

  3. Analytical truth
     latency, slippage, attribution, equity curves, risk metrics, reports

Rules:
  - lower layers feed higher layers only
  - higher layers never mutate lower layers
  - analytics must never become hidden runtime control state
  - every derived metric must be traceable to its source artifact

---

# GLOBAL INVARIANTS (binding contracts -- every system, every strategy)

## Payoff Contract
  Every strategy must define before paper trading begins:
    - Min target move (pips, bps, or USD)
    - Target reach rate: % of trades that hit TP. Kill if < 25%.
    - Timeout rate: % of trades that expire at max hold. Kill if > 60%.
    - Stop rate: % of trades that hit SL. Kill if > 2x target rate.
    - MFE tail check: median MFE must exceed 1.5x cost to confirm real edge.
    - Costs from day one: all backtests and paper runs include realistic fees,
      spread, and slippage. No "add costs later" allowed.

  Template:
    Strategy: _______________
    Venue/Pair: _______________
    Min target move: _______________
    Expected target reach rate: _____ % (kill < 25%)
    Expected timeout rate: _____ % (kill > 60%)
    Expected stop rate: _____ % (kill > 2x target rate)
    Median MFE vs cost: _____ x (kill < 1.5x)
    RT cost model: _____ bps/pips

## Execution Contract
  Every strategy must define:
    - Venue and instrument
    - Fee structure (maker/taker, per-side, RT total)
    - Spread model (median, P95, widening conditions)
    - Slippage model (expected vs worst-case)
    - Fill rate (% of signals that execute)
    - Latency budget (signal-to-fill target)

## Kill Discipline
  - Payoff Contract fail = kill. No exceptions, no "one more week".
  - Execution delta > 25% vs model = kill.
  - 3 consecutive weeks negative with >= 15 trades/week = kill.
  - Drawdown > 3x model (paper) or > 2x model (live) = kill.
  - Kill = archived permanently. Strategy name retired. No resurrection.

## Phase C Rollback Rules
  - ONE variable per cycle. Never two simultaneous experiments.
  - Minimum 5 days or 20 signals per cycle (whichever comes first).
  - Revert if EV drops > 20% vs baseline.
  - No simultaneous experiments across strategies within same system.

---

# SYSTEM PORTFOLIO (think in systems, not markets)

  System 1: Argus Crypto (Coinbase)    -- PASSIVE OBSERVATION (fee-killed, 0/41 configs profitable at 60bps)
  System 2: IBKR Trading Engine        -- PRIMARY ACTIVE (3 viable strategies found)
  System 3: Polymarket                  -- LOCKED (waitlisted, contingency: reassign slot if not off waitlist by 2026-06-01)
  System 4: Equities                   -- NOT STARTED ($25K PDT constraint)

  Rules:
    - Max 2 active systems at any time.
    - Prove one complete edge loop (research -> paper -> live) before expanding.
    - Each system: independent, measurable, scalable.

---

# PROMOTION PIPELINE (Research -> Paper -> Live -> Scale)

## Research -> Paper gate:
  - Payoff Contract defined and passing in backtest
  - 7/7 stress suite pass (gap, spread spike, news, low liquidity, trending,
    ranging, overnight)
  - 14+ days of backtest data minimum
  - 100+ signals in backtest

## Paper -> Live gate:
  - 30+ paper trades (acknowledge this is noisy evidence, not proof)
  - Expectancy within 20% of backtest expectancy
  - Replay-Live Divergence Guard passes (see below)

## Live -> Scale gate:
  - 30+ live fills
  - Slippage within model bounds
  - Expectancy within 30% of paper expectancy

## DEMOTION paths:
  - Live -> Paper: on 20% expectancy degradation (watch trigger, not instant kill)
  - Scale -> Live: on drawdown > 1.5x model

---

# REPLAY-LIVE DIVERGENCE GUARD

  Purpose: Detect when paper/live execution diverges from backtest assumptions.

  Metrics monitored:
    - Entry price delta (backtest vs paper/live)
    - Signal timing delta (bar alignment)
    - Expectancy delta: 20% = alert, 25% = kill
    - Hit rate delta: 10pp = alert, 20pp = kill
    - Timeout rate delta: 15pp = alert

  Status: NOT YET BUILT. Required before any Paper -> Live promotion.

---

# GLOBAL RISK BUDGET

  Allocation limits:
    - Max single system allocation: 50% of capital (paper), 70% (live)
    - No single strategy > 80% of trailing 30-day PnL

  Correlation budget:
    - EUR/USD + GBP/USD count as 1.5 systems, not 2 (correlated USD risk)
    - Correlated pairs share a combined drawdown limit

  Per-system drawdown limits:
    - > 3x model = pause (paper)
    - > 2x model = pause (live)
    - > 1.5x model = reduce scale 50%

  ML Governor rules:
    - Never primary decision path. Advisory only.
    - LOG_ONLY until proven incremental over deterministic rules.
    - Retrain requires 30+ single-config trades. Mixed-config training forbidden.

---

# COMPLETED PHASES (collapsed -- see git history for details)
#   Phase 7:  Backtest determinism + sandbox rule [DONE]
#   Phase 8:  PaperAdapter + fill persistence + startup recovery, Gate A PASS [DONE 2026-03-10]
#   Phase 9:  Trade journal on SELL->FLAT, crash-restart proof [DONE 2026-03-10]
#   Phase 10: Adversarial restart -- 4 kill-point scenarios PASS [DONE 2026-03-10]
#   Phase 11-16: Analytics, risk, attribution, reporting, ops infra [DONE 2026-03-11]
#   Phase 16.5: Strategy optimization -- parameter sweep, 663 backtests [DONE 2026-03-17]
#   Phase 20: WebSocket L2 feed + fast-exit + sub-minute candles [DONE 2026-03-18]
#   ML-1: Governor trained (1,029 trades, ROC-AUC 0.944) [DONE 2026-03-15]
#   Multi-coin: ETH+BTC parallel runners, per-coin isolation [DONE 2026-03-17]
#   v1.1 Cleanup: decision_trace.csv, HOLD taxonomy, OB/vol dedup, governor LOG_ONLY [DONE 2026-03-18]

---

# ===================================================================
# SYSTEM 1: ARGUS CRYPTO (Coinbase) -- PASSIVE OBSERVATION
# ===================================================================

## Status: FEE-KILLED

  The signal is real. Score 90+ delivers 75% WR in backtest. The venue kills it.
  Coinbase 60bps round-trip fees destroy all edge across every tested configuration.

  Payoff Contract: FAILED. Strategy needs 15-20bps RT max to be viable.

  Evidence:
    - 0/41 configs profitable across OOS, walk-forward, and 30-config parameter sweep
    - Penalty analysis (2026-03-20) confirmed governor correctly blocking losing trades
    - TREND_UP underperformance anomaly discovered but irrelevant given fee structure
    - 663-run parameter sweep exhausted all reasonable parameter combinations

  Current state:
    - ETH-USD and BTC-USD runners still live for data collection only
    - Governor in LOG_ONLY mode, stamping all signals for future analysis
    - No Phase C intervention work justified until venue cost changes
    - SOL disabled (PF 0.53-0.64, consistently unprofitable even before fees)

  Infrastructure preserved:
    - Full engine, backtest harness, decision_trace pipeline, dashboard
    - All reusable for future low-cost venue (Kraken, IBKR crypto, DEX)
    - WebSocket feed, fast-exit, sub-minute candles all built and tested

  Reactivation condition: access to venue with < 20bps RT fees for BTC/ETH spot.

---

# ===================================================================
# SYSTEM 2: IBKR TRADING ENGINE -- PRIMARY ACTIVE
# ===================================================================

## Research Status

  ### Closed (killed):
    - Kraken BTC perpetuals (Cascade engine): killed, precursors killed
    - All crypto-venue strategies at current fee structures

  ### Active strategies (all in PAPER phase):

    EUR/USD T4:
      Status: PAPER
      Edge: +0.98 pip/trade expectancy
      Stress: 7/7 suite pass
      Payoff Contract: PASS
      Cost model: < 1bps RT (IBKR FX)

    MNQ vol_burst:
      Status: PAPER
      Edge: +6.41 bps/trade expectancy
      Stress: 7/7 suite pass
      Payoff Contract: PASS
      Cost model: ~3bps RT (IBKR futures)
      Note: using delayed data (15min lag). CME real-time subscription pending.

    GBP/USD range:
      Status: PAPER
      Edge: +0.97 pip/trade expectancy
      Stress: 7/7 suite pass
      Payoff Contract: PASS
      Cost model: < 1bps RT (IBKR FX)

  ### Correlation note:
    EUR/USD + GBP/USD are correlated USD pairs. Per Global Risk Budget,
    they count as 1.5 systems and share a combined drawdown limit.

## IBKR Execution Recovery

  Needed (NOT YET BUILT for IBKR):
    - State files (position, pending orders, trade context)
    - Crash restart with position reconciliation
    - Fill backfill on reconnect
    - Stale order detection and cleanup

## IBKR Infrastructure

  Account: DUP472829 (paper)
  Gateway: TWS Gateway, port 4002

  Runners:
    runner_eurusd.py   (clientId=10)
    runner_mnq.py      (clientId=11)
    runner_gbpusd.py   (clientId=12)

  Configs: argus_flow/configs/*.json (versioned, per-strategy)
  Logs:    argus_flow/logs/{eurusd,mnq,gbpusd}/

  Ops tools:
    smoke_test.py    -- connection + order placement verification
    health_check.py  -- runner liveness + position consistency
    launch_all.ps1   -- start all runners with correct clientIds

  MNQ data note:
    Currently using delayed (15-minute) data. CME Level 1 subscription
    pending. Strategy logic accounts for delay but live execution will
    require real-time feed.

---

# ===================================================================
# SYSTEM 3: POLYMARKET -- LOCKED
# ===================================================================

  Status: LOCKED (waitlisted for account access)
  Why: Inefficient pricing, retail-driven, slow reaction to info, bots proving edge

  Contingency: If not off waitlist by 2026-06-01, reassign this slot to next
  highest-priority system (likely IBKR options or additional FX pairs).

  Strategy types (planned):
    A. News latency edge: event happens -> price lags -> buy before correction
    B. Probability mispricing: model probability > market probability -> buy
    C. Cross-market arb: related markets inconsistent -> exploit spread

  Architecture (planned):
    Data: news APIs, twitter/X scraping, event feeds
    Model: probability estimator, sentiment + keyword scoring
    Execution: Polymarket API, track odds vs model
    Risk: cap exposure per event, decay positions near resolution

  Build phases (when unlocked):
    Phase 1: Scrape markets, track price changes, detect anomalies
    Phase 2: Simple probability model, compare vs market
    Phase 3: Paper trading
    Phase 4: Live with micro-capital

  Integration with Argus: NONE (separate system, separate codebase)

---

# ===================================================================
# SYSTEM 4: EQUITIES -- NOT STARTED
# ===================================================================

  Status: NOT STARTED
  Constraint: PDT rule (< $25K -> no frequent day trading)
  Capital path: No accumulation path currently. Revisit when IBKR live
  generates enough to cross $25K threshold.

  Best path: Swing trading (1-3 day holds, no PDT issues)
  Broker: IBKR (better API than Robinhood)

  Strategy:
    Gap + momentum continuation
    Earnings/news driven moves
    Premarket movers, volume spikes, news catalysts

  Partial reuse from Argus: confluence scoring concept, risk framework
  Different: data, timing, volatility, hold periods

---

# INFRASTRUCTURE (ALL DONE -- collapsed)

## Ops & Analysis Tools  [ALL DONE 2026-03-13/14]
  PC2 dispatch, run comparison, score/duration analyzer, post-backtest compare,
  job queue (PC1+PC2), unified status, auto-compare leaderboard, artifact cleanup,
  Discord alerts (backtest/trade/error/runner), equity overlay chart, trade export,
  queue watcher, post-queue report, dashboard (FastAPI+SSE+Chart.js), architecture map,
  backup verification, candle gap alerting, weekly digest, DR runbook,
  dashboard queue panel, dashboard trade journal viewer.

## Strategy Features Built  [ALL DONE]
  Session filtering, multi-asset backtest, compounding rules, drawdown circuit breaker,
  candle data validation, time-stop exit, 4hr candle aggregation.

## Survivability & Recovery  [ALL DONE]
  Git auto-backup (daily@02:00), USB nightly backup (daily@03:00),
  backup verification drill (weekly Sunday@04:00), disaster recovery runbook.

---

# TWO-MACHINE SETUP

  PC1 (WINDOWS-5RCTEK3): runner_live.py 24/7, dev work, heavy backtests
  PC2 (DESKTOP-17CJMUP / 192.168.1.98): offloaded backtest jobs
  GitHub: Ksmith2322/Argus, branch phase6-hardening
  SSH: passwordless ED25519 key, dispatch via ops/dispatch_backtest_pc2.ps1
  .env tracked in git (paper trading, no real keys)
  ops/logs/ NOT in git -- artifacts are local to each machine

---

# SCHEDULED TASKS (PC1)

  ArgusGitBackup:       daily@02:00  -> ops/auto_git_backup.ps1
  Argus_Nightly_Backtest: daily@02:10 -> ops/run_backtest.ps1
  ArgusUSBBackup:       daily@03:00  -> ops/backup_to_usb.ps1
  ArgusRefreshCandles:  Sunday@03:00 -> ops/refresh_candles.ps1 (ETH+BTC+SOL, 30d)
  ArgusVerifyBackup:    Sunday@04:00 -> ops/verify_backup.ps1 (weekly integrity check)
  ArgusWeeklyDigest:    Sunday@06:00 -> ops/weekly_digest.py (Discord performance report)
  Runner auto-start:    on login     -> Startup folder -> ops/autostart_runner.ps1
  Dashboard auto-start: on login     -> Startup folder -> ops/autostart_dashboard.ps1

---

# THE SCALING PATH

  Phase 1 (NOW): Prove EV (paper) -- IBKR runners live, accumulate 30+ trades each
  Phase 2: Prove execution (micro live) -- real capital, real fills, real slippage
  Phase 3: Prove stability -- N trades with bounded drawdown within model
  Phase 4: Scale capital into proven edges
  Phase 5: Add systems (only after current systems are profitable)

  Rule: stacking proven edges and scaling capital into them > adding more systems