# ARGUS ROADMAP
# Last updated: 2026-03-14
# Goal: fully operational autonomous trading system, no outside interaction required,
#        visual performance monitoring built-in, fail-closed behavior, human-governed
#        promotion boundaries.

---

# TRUTH-SURFACE DOCTRINE (architectural rule — never violate)

Argus has three truth classes:

  1. Canonical execution truth
     orders, fills, account, positions, decision events

  2. Deterministic lifecycle truth
     trade lifecycle records rebuilt from canonical truth (trade_journal)

  3. Analytical truth
     latency, slippage, attribution, equity curves, risk metrics, reports

Rules:
  - lower layers feed higher layers only
  - higher layers never mutate lower layers
  - analytics must never become hidden runtime control state
  - every derived metric must be traceable to its source artifact

---

# COMPLETED PHASES (collapsed — see git history for details)
#   Phase 7:  Backtest determinism + sandbox rule [DONE]
#   Phase 8:  PaperAdapter + fill persistence + startup recovery, Gate A PASS [DONE 2026-03-10]
#   Phase 9:  Trade journal on SELL->FLAT, crash-restart proof [DONE 2026-03-10]
#   Phase 10: Adversarial restart — 4 kill-point scenarios PASS [DONE 2026-03-10]
#   Phase 11-16: Analytics, risk, attribution, reporting, ops infra [DONE 2026-03-11]
#     (DATA PENDING — modules populate as live fills accumulate; need 30-50+ fills)

## Phase 16.5 — Strategy Optimization  [IN PROGRESS]

### Bugs Fixed (code in place):
  1. Liq filter unit mismatch — ETH vol vs USD floor. Fixed: LIQ_MIN_VOL_UNITS_1M=25 ETH.
  2. PENALIZE mode penalty never applied — early return bug. Fixed: penalty applied first.
  3. TL_PENALTY_RESIST_SLOPE_NEG feature added (confluence.py + config.py + .env).

### Old Baseline (2026-03-12, dataset Feb 9–Mar 11, 43K candles):
  WR=25.35%  PF=0.74  trades=71  PnL=-$5.05
  Liq fix test: neutral. Trendline bonus test: WORSE (bonuses promote marginal entries).
  VERDICT: FAIL — bear/chop dataset, not a filter problem.

### Fresh Baseline (2026-03-13, dataset Feb 11–Mar 13, 43K candles):
  run_id: bt_20260313T060439Z_9518a56d (determinism confirmed, Run 1 = Run 2)
  77 trades, 24.68% WR, PF=0.74, PnL=-$5.92, avg_win=$0.52, avg_loss=$0.23 (R:R 2.2:1)
  Consistent with old baseline — strategy is stable but underwater.

### Score Analysis (fresh baseline):
  Score < 75:  33 trades, 15.2% WR, PnL=-$5.90  (LOSING)
  Score 75-79: 13 trades,  7.7% WR, PnL=-$1.52  (LOSING)
  Score 80-84: 16 trades, 18.8% WR, PnL=-$2.02  (LOSING)
  Score 85-89:  3 trades, 33.3% WR, PnL=+$0.19  (BREAK-EVEN)
  Score 90+:   12 trades, 75.0% WR, PnL=+$5.71  (WINNING)

  RANGE regime: 9 trades, 0% WR, PnL=-$2.20 (ALL LOSSES)
  TREND_UP:    68 trades, 27.9% WR, PnL=-$1.34

### Duration Analysis (strongest alpha signal — confirmed across both runs):
  <30m:     27 trades, 70.4% WR, PnL=+$11.75  (MASSIVE WINNER)
  30m-1.5h: 28 trades,  3.6% WR, PnL=-$7.51   (DEAD ZONE — all losers)
  1.5-3h:   36 trades,  2.8% WR, PnL=-$7.77   (DEAD ZONE)
  >3h:      44 trades, 22.7% WR, PnL=-$2.95   (slight recovery)

### Time-Stop Exit (implemented 2026-03-13):
  MAX_HOLD_SECONDS=5400 (90 min). Closes position if held too long.
  Priority: STOP_LOSS > MIN_HOLD > TIME_STOP > TREND_INVALIDATION > TRAIL > TP.

### Queued Optimization Tests (8 jobs, running on PC1+PC2 in parallel ~18h):
  PC1 queue (ops/backtest_queue.jsonl):
    1. 90min-timestop_score90_regime
    2. 30min-timestop_score90_regime
    3. 30min-timestop_score85_regime
  PC2 queue (ops/backtest_queue_pc2.jsonl):
    4. 30min-trendline-penalties_score80_regime
    5. btc-usd_30min_score80_regime (cross-asset)
    6. sol-usd_30min_score80_regime (cross-asset)
    7. compound-5pct_30min_score80_regime

  Queue watcher (watch_and_launch_queue.ps1) running — auto-launches when in-flight BTs finish.
  Post-queue report (post_queue_report.py) auto-sends leaderboard to Discord on completion.

### Next steps:
  1. Queue finishes → review Discord leaderboard
  2. Identify winning config from post_queue_report.py output
  3. Lock winning config, restart PC1 live runner
  4. Phase 16.5 EXIT when backtest confirms WR>=35% AND PF>=1.20

---

# ACTIVE ROADMAP

## Phase 17 — Paper Capital Proof  [NEXT — after Phase 16.5 passes]

Goal: Prove paper growth under unattended operation with optimized config.

Rules (hard):
  * Fixed config, locked at run start (config hash enforced)
  * No strategy edits during run
  * No operator overrides except KILL_SWITCH or PAUSE
  * Any halt is logged and explainable from artifacts

Duration: minimum 4 weeks unattended

Exit:
  * Paper growth inside modeled expectancy and drawdown bounds
  * Reconciliation passes every run
  * 30+ live fills accumulated (unlocks Phase 11 data gate)

## Phase 18 — Real Micro-Capital Validation  [AFTER PHASE 17]

Goal: Validate live settlement, fees, slippage under real capital.

Build:
  * execution/coinbase_adapter.py — real adapter (same interface as PaperAdapter)
  * Model vs reality deviation tracker

Rules:
  * Minimal capital (defined before starting)
  * Tight instrumentation
  * Live behavior must stay inside friction model bounds

Exit:
  * Live behavior inside expected bounds
  * Real slippage matches empirical friction model
  * Live reconciliation passes consistently

---

# INFRASTRUCTURE DONE (collapsed)

## Ops & Analysis Tools  [ALL DONE 2026-03-13/14]
  [DONE] PC2 dispatch reliability — schtasks detachment + log capture
  [DONE] Run comparison tool — ops/compare_runs.py
  [DONE] Score/duration analyzer — ops/analyze_scores.py
  [DONE] Post-backtest auto-compare — ops/post_backtest_compare.ps1
  [DONE] Job queue — ops/run_queue.ps1 + backtest_queue.jsonl
  [DONE] PC2 queue dispatch — ops/dispatch_queue_pc2.ps1
  [DONE] Unified status script — ops/status_all.ps1
  [DONE] Auto-compare leaderboard — ops/auto_compare.py
  [DONE] Artifact cleanup — ops/cleanup_artifacts.ps1
  [DONE] Discord alerts — ops/notify.py (backtest complete, trade close, error, runner start/crash)
  [DONE] Equity overlay chart — ops/plot_equity_overlay.py
  [DONE] Trade export tool — ops/export_trades.py
  [DONE] Queue watcher — ops/watch_and_launch_queue.ps1
  [DONE] Post-queue report — ops/post_queue_report.py
  [DONE] Dashboard — ops/dashboard.py (FastAPI + SSE + Chart.js, localhost:8080)
  [DONE] Architecture map — ops/generate_map.py -> ARGUS_MAP.mermaid
  [DONE] Backup verification — ops/verify_backup.ps1

## Strategy Features Built  [ALL DONE]
  [DONE] Session filtering — SESSION_ENTRY_BLOCK_LIST
  [DONE] Multi-asset backtest — BACKTEST_SYMBOL env var
  [DONE] Compounding rules — COMPOUND_SIZE_PCT
  [DONE] Drawdown circuit breaker — DRAWDOWN_PAUSE_PCT
  [DONE] Candle data validation — ops/validate_candles.py
  [DONE] Time-stop exit — MAX_HOLD_SECONDS
  [DONE] 4hr candle aggregation — CandleBuilder(14400) + 50-candle rolling buffer in state/engine

## Risk & Safety  [ALL DONE]
  [DONE] Drawdown circuit breaker (DRAWDOWN_PAUSE_PCT=0.03)
  [DONE] Candle data validation (ops/validate_candles.py)

---

# FUTURE PRIORITIES

## Priority 1 — Strategy Edge  [AFTER PHASE 16.5]
  [TEST] Trendline penalties only (disable bonuses, keep -8/-15 penalties + slope_neg)
  [TODO] Expand to BTC-USD after ETH edge confirmed
  [TODO] Adaptive score threshold — auto-adjust CONFLUENCE_MIN_SCORE based on rolling WR
  [TODO] Volume-weighted entry timing — prefer entries at volume spikes vs quiet periods
  [TODO] Fake-out detection & mitigation
    - Identify entries that reverse quickly after fill (e.g. price drops X% within N bars)
    - Possible signals: wick rejection, volume divergence at entry, order book imbalance
    - Could add a "confirmation delay" — wait N bars after signal before entering
    - Track fake-out rate per score bucket / regime / session to find patterns
    - Related: the 30m-3h dead zone is likely dominated by fake-outs
  [TODO] Liquidation-aware entry gating
    - Monitor large liquidation events by size (whale liquidations vs retail)
    - Block entries near large liquidation cascades (momentum is unreliable)
    - Possibly use liquidations AS signals — large liq clusters can mark reversals
    - Needs: liquidation data feed or estimation from price/volume spikes
  [TODO] Short selling support
    - Currently long-only; adding shorts doubles opportunity set
    - Engine: support SELL-to-open (short entry) + BUY-to-close (short exit)
    - Ledger: track negative positions, short PnL = (entry - exit) * qty
    - PaperAdapter: simulate short fills (same interface)
    - Confluence: may need separate score thresholds for shorts vs longs
    - Risk: separate drawdown limits for short book
    - Phase: implement after long-only edge is proven (Phase 17+)
  [TODO] Pyramiding / add-to-winners
    - When position is profitable and trend continues, add to position size
    - Rules: only add if unrealized PnL > X%, max N additions, trailing stop on full position
    - Exit: close entire position at once (not partial — sell whole stack)
    - Needs: multi-fill position tracking in ledger (currently single entry/exit)
    - Risk: larger position = larger drawdown if reversal — needs tight trailing stop
  [TODO] Mean-reversion strategy
    - Opposite of current momentum/confluence approach
    - Entry: price deviates X std devs from moving average → enter expecting reversion
    - Signals: RSI oversold/overbought, Bollinger Band extremes, volume exhaustion
    - Could run alongside momentum strategy (strategy coordination layer)
    - Best in RANGE regime (where momentum strategy loses)
    - Needs: separate backtest validation, own score/confluence path
    - Phase: build as second strategy, test independently, then coordinate

## Priority 2 — Multi-Timeframe Strategy  [TODO — foundation built]
  [DONE] 4hr candle aggregation in engine (CandleBuilder + rolling buffer)
  [TODO] S/R trendline detection on 4hr chart (pivot highs/lows → projected lines)
  [TODO] Entry signals based on 4hr S/R bounces/breaks
  [TODO] Sliding window position sizing (rolling WR/PF per signal → size scaling)
  [TODO] Strategy coordination layer (1m confluence + 4hr swing coexistence)

## Priority 3 — Survivability & Recovery
  [DONE] Backup verification drill — ops/verify_backup.ps1 (weekly, Sunday@04:00)
  [DONE] Git auto-backup — ops/auto_git_backup.ps1 (daily@02:00)
  [DONE] USB nightly backup — ops/backup_to_usb.ps1 (scheduled ArgusUSBBackup daily@03:00)
    - robocopy /MIR to USB E:\Argus_Backup, excludes __pycache__/.git/objects/*.pyc
    - Logs: last 7 days only. Key file verification. Discord alert on success/failure.
  [TODO] Disaster recovery runbook — document full restore from USB + GitHub

## Priority 4 — Observability & Monitoring
  [TODO] Dashboard queue panel — show current queue status, running job, ETA
  [TODO] Dashboard trade journal viewer — browse closed trades with filters
  [TODO] Candle gap alerting — proactive Discord alert when feed gaps detected
  [TODO] Weekly performance digest — auto-generate and Discord-send weekly PnL/WR summary

## Priority 5 — ML as Governor  [TODO — needs 500+ trades across all assets]
  * 500 trades = combined ETH + BTC + SOL (backtest + live)
  * Current queue includes BTC-USD and SOL-USD runs — those trades count
  * Regime classifier: predicts TREND/RANGE/VOLATILE from 1h features
  * Anomaly detector: flags unusual fill patterns or execution drift
  * Capital allocation ranker: rank entry opportunities by predicted quality
  * NEVER replaces deterministic engine — acts as score modifier only

---

# FUTURE EXTENSIONS (after Phase 18)

  * Champion-challenger shadow runtime
  * Dual-feed market data validation
  * Hash-chained artifact integrity
  * Deterministic replay certification
  * Multi-venue portability (perpetual futures)
  * Policy engine above strategy engine
  * Regime black-swan library
  * Storage and recovery maturity
  * Multi-exchange arbitrage monitoring
  * API rate limit / circuit breaker hardening

---

# TWO-MACHINE SETUP

  PC1 (WINDOWS-5RCTEK3): runner_live.py 24/7, dev work, heavy backtests
  PC2 (DESKTOP-17CJMUP): offloaded backtest jobs
  GitHub: Ksmith2322/Argus, branch phase6-hardening
  SSH: passwordless ED25519 key, dispatch via ops/dispatch_backtest_pc2.ps1
  .env tracked in git (paper trading, no real keys)
  ops/logs/ NOT in git — artifacts are local to each machine

---

# TRENDLINE ENGINE (currently disabled)

  TrendlineEngine: detects pivot highs/lows on 1h candles, projects descending resistance
  and ascending support trendlines, computes proximity and breakout signals.
  Integrated into state.py, engine.py, confluence.py, decisions.py, config.py.

  Score effects (when USE_TRENDLINES=true):
    Near support trendline:      +5 to score (bonus)
    Near resistance trendline:   -8 to score (penalty)
    Broke above resistance:     +12 to score (bonus)
    Broke below support:        -15 to score + HARD BLOCK
    Resistance slope negative:   -5 to score (TL_PENALTY_RESIST_SLOPE_NEG)

  Status: USE_TRENDLINES=false. Bonuses were counterproductive in 2026-03-12 test.
  Next test: penalties only with TL_PENALTY_RESIST_SLOPE_NEG=5 (queued on PC2).

---

# SCHEDULED TASKS (PC1)

  ArgusRefreshCandles:  Sunday@03:00 -> ops/refresh_candles.ps1 (ETH+BTC+SOL, 30d)
  ArgusGitBackup:       daily@02:00  -> ops/auto_git_backup.ps1
  ArgusNightlyBT:       (Task Scheduler) -> ops/run_backtest.ps1
  Runner auto-start:    on login     -> Startup folder -> ops/autostart_runner.ps1
  Dashboard auto-start: on login     -> Startup folder -> ops/autostart_dashboard.ps1
  ArgusVerifyBackup:    Sunday@04:00 -> ops/verify_backup.ps1 (weekly integrity check)
  ArgusUSBBackup:       daily@03:00  -> ops/backup_to_usb.ps1 (DONE — mirrors to USB E:\Argus_Backup)