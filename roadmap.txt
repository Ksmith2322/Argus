# ARGUS ROADMAP
# Last updated: 2026-03-18
# Goal: Build multiple independent, automatable edge engines.
# Each must have measurable EV, be automatable, and scale capital.
# Strategy: narrow → prove → scale → expand

---

# TRUTH-SURFACE DOCTRINE (architectural rule — never violate)

Argus has three truth classes:

  1. Canonical execution truth
     orders, fills, account, positions, decision events, decision_trace

  2. Deterministic lifecycle truth
     trade lifecycle records rebuilt from canonical truth (trade_journal)

  3. Analytical truth
     latency, slippage, attribution, equity curves, risk metrics, reports

Rules:
  - lower layers feed higher layers only
  - higher layers never mutate lower layers
  - analytics must never become hidden runtime control state
  - every derived metric must be traceable to its source artifact

---

# SYSTEM PORTFOLIO (think in systems, not markets)

  System 1: Momentum engine (Argus crypto)      — PRIMARY, ACTIVE
  System 2: Event arb engine (Polymarket)        — BUILD NEXT
  System 3: Validation engine (Forex via IBKR)   — INFRA ONLY
  System 4: News momentum engine (Stocks/Swing)  — LATER

  Each system: independent, measurable, scalable.
  Rule: prove one edge loop end-to-end before expanding surface area.

---

# COMPLETED PHASES (collapsed — see git history for details)
#   Phase 7:  Backtest determinism + sandbox rule [DONE]
#   Phase 8:  PaperAdapter + fill persistence + startup recovery, Gate A PASS [DONE 2026-03-10]
#   Phase 9:  Trade journal on SELL->FLAT, crash-restart proof [DONE 2026-03-10]
#   Phase 10: Adversarial restart — 4 kill-point scenarios PASS [DONE 2026-03-10]
#   Phase 11-16: Analytics, risk, attribution, reporting, ops infra [DONE 2026-03-11]
#   Phase 16.5: Strategy optimization — parameter sweep, 663 backtests [DONE 2026-03-17]
#   Phase 20: WebSocket L2 feed + fast-exit + sub-minute candles [DONE 2026-03-18]
#   ML-1: Governor trained (1,029 trades, ROC-AUC 0.944) [DONE 2026-03-15]
#   Multi-coin: ETH+BTC parallel runners, per-coin isolation [DONE 2026-03-17]
#   v1.1 Cleanup: decision_trace.csv, HOLD taxonomy, OB/vol dedup, governor LOG_ONLY [DONE 2026-03-18]

---

# ===================================================================
# SYSTEM 1: ARGUS CRYPTO MOMENTUM ENGINE (PRIMARY)
# ===================================================================

## Current State (v1.1_cleanup_pass)

  Config: score88, trendlines ON, structure OFF, 60min hold, 10% compound
  Governor: LOG_ONLY (stamps all, blocks none — on probation)
  Coins: ETH-USD, BTC-USD (SOL disabled — unprofitable)
  Mode: Paper trading, $500/coin, ~$50/trade
  Trace: decision_trace.csv LIVE — collecting forensic data

### Key Findings (663-run sweep):
  Score 90+: 75% WR, clear edge
  RANGE regime: 0% WR (graveyard)
  Duration <30m: 70% WR (massive winner)
  Duration 30m-3h: 3% WR (dead zone)
  OVERLAP session: best. OFF session: worst.
  Only ETH and BTC viable — all 15 alts PF < 0.82

## Phase A — Validate the Trace (NOW — in progress)

  Goal: Confirm decision_trace.csv is complete and correct.

  Checks:
    * every entry-eligible tick writes a row
    * blockers populated consistently
    * gate transitions not blank when score/gate changes
    * CONFIG_VERSION present everywhere
    * LOG_ONLY ML output stamped on all relevant rows
    * no hidden fallback bucket under new hold reasons

## Phase B — Edge Extraction (after 200+ trace rows, 20+ in 88+ zone, 10+ trades)

  Review gate (minimum before analysis):
    * 200+ decision trace rows
    * 20+ candidates in the 88+ zone
    * 10+ taken trades
    * single config version (v1.1_cleanup_pass)
    * if 5-7 days arrives first, review anyway

  ### B.1 Hard blocker frequency (Table A)
    Hard Blocker | Count | % of >=88 rows blocked | Avg blocked score
    (risk lockout, cooldown, qty zero, liquidity fail, cross-coin, governor)

  ### B.2 Score suppressor frequency (Table B)
    Score Suppressor | Mean delta | Count on blocked >=88 candidates | Avg base→final
    (session penalty, OB penalty, BTC lag, structure/trendline deltas)

  ### B.3 Score × Regime EV matrix
    Buckets (threshold-centric, frozen threshold=88):
      Regime | 70-79 | 80-87 (near-miss) | 88-91 (tradable) | 92+ (high-conviction)
    Cols: TREND_UP, RANGE, TREND_DOWN (collapse VOLATILE if sparse)
    Metrics: trade count, win rate, EV, median hold time

  ### B.4 Score × Session EV matrix
    Same score buckets × OVERLAP, NY, LONDON, ASIA, OFF

  ### B.5 Blocked-high-score analysis
    For each blocked row with final_score >= 88 (or base_score >= 88):
      base_score, final_score, base_gate, final_gate
      hard_blockers (list), score_suppressors (list with deltas)
      regime, session, action_reason
    → identifies whether leak is direct veto or death by accumulated penalties

  ### B.6 Taken trade review
    For each completed trade:
      score, regime, session, duration, MAE/MFE, exit reason, PnL
    → confirms edge zones match backtest findings

## Phase C — First Intervention Ladder (after Phase B read)

  Rule: change ONE thing, remeasure. Not multiple simultaneous changes.

  ### C.1 Relax one soft policy constraint (first — likely highest ROI)
    Likely candidates (pick based on Phase B data):
      - session penalty weaker for 88+ TREND candidates
      - OB penalty cap reduced
      - MTF hard confirmation softened or converted to modifier in TREND only

  ### C.2 Coarse sizing multipliers (second — capital concentration)
    Not smooth EV sizing yet. Start with a grid:
      TREND + 92+      → 1.25x
      TREND + 88-91    → 1.00x
      RANGE + 92+      → 0.60x
      RANGE + 88-91    → 0.35x
      OFF session      → 0.70x overlay

  ### C.3 Shadow time-pressure exit (third — dead-zone suppression)
    Shadow only first (log would-have-exited, don't actually exit):
      RANGE + score<92 + no progress after X minutes → would-exit
    Compare would-have-been vs actual
    Only activate after shadow proves value

  ### C.4 Harder regime suppression
    If RANGE kills even high scores → hard suppress or 0.25x size
    Test empirically from Phase B data, not by intuition
    Exploits the short-duration advantage

  ### C.3 Harder regime suppression
    If RANGE kills even high scores → hard suppress or 0.25x size
    Test empirically, not by intuition

## Phase D — Strategic Decisions (after Phase B/C data)

  ### D.1 MTF confirmation: score-only vs confirmation-doctrine
    A/B test: hard MTF on vs off
    Compare EV, trade count, drawdown by score bucket
    → philosophy decision, not cleanup

  ### D.2 ML governor mode: LOG_ONLY vs SCORE_MODIFY vs GATE
    Only promote if proven incremental edge over deterministic rules
    Risk: model becomes de facto brain, deterministic layer becomes ceremony
    → secondary policy, not core signal truth

  ### D.3 BTC overlays: lag vs momentum — which adds edge
    Different timescales, not true duplicates
    Evaluate after attribution data exists
    → consolidate only after empirical comparison

  ### D.4 Governor retrain
    Must use single-config live trade data (30+ per coin)
    Mixed-config training produces ROC-AUC 0.500 (useless)

## Phase 17 — Paper Capital Proof (after Phase C optimizations)

  Goal: Prove paper growth under unattended operation with optimized config.
  Duration: minimum 4 weeks unattended
  Rules:
    * Fixed config, locked at run start (config hash enforced)
    * No strategy edits during run
    * No operator overrides except KILL_SWITCH or PAUSE
  Exit:
    * Paper growth inside modeled expectancy and drawdown bounds
    * Reconciliation passes every run
    * 30+ live fills accumulated

## Phase 18 — Real Micro-Capital Validation (after Phase 17)

  Goal: Validate live settlement, fees, slippage under real capital.
  Build: execution/coinbase_adapter.py (or IBKR adapter)
  Rules:
    * Minimal capital
    * Live behavior must stay inside friction model bounds
  Exit:
    * Real slippage matches empirical friction model
    * Live reconciliation passes consistently

## Phase 19 — Multi-Asset Expansion (after edge proven)

  Add SOL, possibly LINK — same engine, minimal changes
  ONLY after knowing where edge lives (score/regime/session)
  Rule: same strategy, different symbols. Not new strategies.

## Strategy Features (built, available for activation)

  [BUILT] WebSocket L2 feed + fast-exit (500ms OB checks)
  [BUILT] ATR dynamic stops (scale TP/SL by volatility)
  [BUILT] Conviction sizing (70%/100%/140% by score tier)
  [BUILT] Session scoring (OVERLAP +8, NY +5, ASIA -5, OFF -8)
  [BUILT] MTF confirmation (5m trend alignment gate)
  [BUILT] Adaptive cooldown (50% after wins, 200% after losses)
  [BUILT] Partial TP ladder (34% at 1R, 50% at 1.5R, rest rides trail)
  [BUILT] Cross-coin guard (max concurrent positions)
  [BUILT] Limit orders via WS bid/ask (ready for IBKR)
  [BUILT] Sub-minute candle aggregation from WS ticker
  [BUILT] OB imbalance score shaping (+8/+15 bull, -12 bear)

  [TODO] Short selling support (after IBKR approved)
  [TODO] Pyramiding / add-to-winners (after 30+ live trades)
  [TODO] Mean-reversion strategy for RANGE regime

---

# ===================================================================
# SYSTEM 2: POLYMARKET EVENT ARBITRAGE ENGINE (BUILD NEXT)
# ===================================================================

  Status: NOT STARTED
  Why: Inefficient pricing, retail-driven, slow reaction to info, bots proving edge

  Strategy types:
    A. News latency edge: event happens → price lags → buy before correction
    B. Probability mispricing: model probability > market probability → buy
    C. Cross-market arb: related markets inconsistent → exploit spread

  Architecture:
    Data: news APIs, twitter/X scraping, event feeds
    Model: probability estimator, sentiment + keyword scoring
    Execution: Polymarket API, track odds vs model
    Risk: cap exposure per event, decay positions near resolution

  Build phases:
    Phase 1: Scrape markets, track price changes, detect anomalies
    Phase 2: Simple probability model, compare vs market
    Phase 3: Paper trading
    Phase 4: Live with micro-capital

  Integration with Argus: NONE (separate system, separate codebase)

---

# ===================================================================
# SYSTEM 3: FOREX VALIDATION ENGINE (IBKR — INFRA ONLY)
# ===================================================================

  Status: WAITING ON IBKR APPROVAL (DUP472829)
  Role: Validation engine — tests if edge generalizes to slower markets

  Now: Connect, fetch data, place paper trades. NO strategy work yet.
  Later: Stripped Argus (no OB, no BTC logic), heavy session weighting

  Build phases:
    Phase 1: IBKR API connection + paper order placement
    Phase 2: Data feed validation (tick/candle quality)
    Phase 3: Stripped Argus strategy, paper only
    Phase 4: Compare EV vs crypto — does the edge transfer?

---

# ===================================================================
# SYSTEM 4: EQUITIES NEWS MOMENTUM ENGINE (LATER)
# ===================================================================

  Status: NOT STARTED
  Constraint: PDT rule (<$25K → no frequent day trading)

  Best path: Swing trading (1-3 day holds, no PDT issues)
  Broker: IBKR (better API than Robinhood)

  Strategy:
    Gap + momentum continuation
    Earnings/news driven moves
    Premarket movers, volume spikes, news catalysts

  Partial reuse from Argus: confluence scoring concept, risk framework
  Different: data, timing, volatility, hold periods

---

# INFRASTRUCTURE (ALL DONE — collapsed)

## Ops & Analysis Tools  [ALL DONE 2026-03-13/14]
  PC2 dispatch, run comparison, score/duration analyzer, post-backtest compare,
  job queue (PC1+PC2), unified status, auto-compare leaderboard, artifact cleanup,
  Discord alerts (backtest/trade/error/runner), equity overlay chart, trade export,
  queue watcher, post-queue report, dashboard (FastAPI+SSE+Chart.js), architecture map,
  backup verification, candle gap alerting, weekly digest, DR runbook,
  dashboard queue panel, dashboard trade journal viewer.

## Strategy Features Built  [ALL DONE]
  Session filtering, multi-asset backtest, compounding rules, drawdown circuit breaker,
  candle data validation, time-stop exit, 4hr candle aggregation.

## Survivability & Recovery  [ALL DONE]
  Git auto-backup (daily@02:00), USB nightly backup (daily@03:00),
  backup verification drill (weekly Sunday@04:00), disaster recovery runbook.

---

# TWO-MACHINE SETUP

  PC1 (WINDOWS-5RCTEK3): runner_live.py 24/7, dev work, heavy backtests
  PC2 (DESKTOP-17CJMUP / 192.168.1.98): offloaded backtest jobs
  GitHub: Ksmith2322/Argus, branch phase6-hardening
  SSH: passwordless ED25519 key, dispatch via ops/dispatch_backtest_pc2.ps1
  .env tracked in git (paper trading, no real keys)
  ops/logs/ NOT in git — artifacts are local to each machine

---

# SCHEDULED TASKS (PC1)

  ArgusGitBackup:       daily@02:00  -> ops/auto_git_backup.ps1
  Argus_Nightly_Backtest: daily@02:10 -> ops/run_backtest.ps1
  ArgusUSBBackup:       daily@03:00  -> ops/backup_to_usb.ps1
  ArgusRefreshCandles:  Sunday@03:00 -> ops/refresh_candles.ps1 (ETH+BTC+SOL, 30d)
  ArgusVerifyBackup:    Sunday@04:00 -> ops/verify_backup.ps1 (weekly integrity check)
  ArgusWeeklyDigest:    Sunday@06:00 -> ops/weekly_digest.py (Discord performance report)
  Runner auto-start:    on login     -> Startup folder -> ops/autostart_runner.ps1
  Dashboard auto-start: on login     -> Startup folder -> ops/autostart_dashboard.ps1

---

# THE SCALING PATH

  Phase 1 (NOW): Argus becomes profitable — identify edge zones via trace
  Phase 2: Increase size in edge zones, add 1-2 crypto assets
  Phase 3: Polymarket starts generating side income
  Phase 4: Equities system adds another stream
  Phase 5: Capital scaling across proven systems

  Rule: stacking proven edges and scaling capital into them > adding more systems